/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}"], // Ensure Tailwind scans all React components
    theme: {
      extend: {}, // You can add custom themes here if needed
    },
    plugins: [], // You can add Tailwind plugins here if required
  };
  